{
  "type": "module",
  "name": "databuddy",
  "version": "0.0.1",
  "description": "CSV data helper",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "browserify turf_browserify.js -s turf > turfBundle.js "
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@turf/buffer": "^6.3.0",
    "@turf/points-within-polygon": "^6.3.0"
  },
  "devDependencies": {
    "browserify": "^17.0.0"
  }
}
